data;


param cardinality := 256;

for{a in I_1}{
let  p[a] := 1/(2*(cardinality-1));
}
let p[cardinality] := 1/2;
param entropy=sum{i in I}(-p[i]*log(p[i])/log(2));
param vare=(sum{i in I}(p[i]*(log(p[i]))^2/log(2)^2)) -entropy^2;